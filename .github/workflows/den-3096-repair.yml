name: DEN-3096 one-shot analyzer repair

on:
  push:
    branches:
      - feat/custom-borrow-checker
    paths:
      - .github/workflows/den-3096-repair.yml

permissions:
  contents: write

concurrency:
  group: den-3096-one-shot-${{ github.ref }}
  cancel-in-progress: false

jobs:
  repair:
    if: >-
      github.repository == 'flags-2-env/flags-2-env' &&
      github.ref == 'refs/heads/feat/custom-borrow-checker'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out the exact repair carrier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: feat/custom-borrow-checker
          fetch-depth: 2
          persist-credentials: true
          show-progress: false

      - name: Require the reviewed carrier ancestry and checker blob
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git rev-parse HEAD^)" = "d780aee5d1f1bcb3445af791da9475882292b0b9"
          test "$(git hash-object tools/borrow-checker/borrow_check.py)" = \
            "e00047d734ffa1b43e5d3415e33d6d12425b4d06"
          test -f .github/repair/den-3096.py
          test -f .github/repair/den-3096-post.py
          test -f .github/repair/den-3096-carrier-fix.py

      - name: Align and apply the exact-blob repair
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            python3 .github/repair/den-3096-carrier-fix.py
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            python3 .github/repair/den-3096.py
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            python3 .github/repair/den-3096-post.py

      - name: Compile and exercise the repaired checker
        shell: bash
        run: |
          set -euo pipefail
          command -v clang
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            python3 -m py_compile tools/borrow-checker/borrow_check.py
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            python3 tools/borrow-checker/borrow_check.py \
              --clang clang \
              --self-test
          PYTHONPYCACHEPREFIX="$RUNNER_TEMP/python-cache" \
            bash scripts/borrow-check.sh
          git diff --check

      - name: Remove the write-capable carrier and commit only reviewed outputs
        shell: bash
        run: |
          set -euo pipefail
          rm \
            .github/repair/den-3096.py \
            .github/repair/den-3096-post.py \
            .github/repair/den-3096-carrier-fix.py
          rmdir .github/repair || true
          git rm .github/workflows/den-3096-repair.yml
          git add -A
          test ! -e .github/repair/den-3096.py
          test ! -e .github/repair/den-3096-post.py
          test ! -e .github/repair/den-3096-carrier-fix.py
          test ! -e .github/workflows/den-3096-repair.yml
          ! git ls-files --error-unmatch .github/repair/den-3096.py >/dev/null 2>&1
          ! git ls-files --error-unmatch .github/repair/den-3096-post.py >/dev/null 2>&1
          ! git ls-files --error-unmatch .github/repair/den-3096-carrier-fix.py >/dev/null 2>&1
          ! git ls-files --error-unmatch .github/workflows/den-3096-repair.yml >/dev/null 2>&1
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix(analyzer): close nullability and waiver bypasses'
          git push origin HEAD:feat/custom-borrow-checker
